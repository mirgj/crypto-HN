extends ./common/layout.pug
include ./common/mixins.pug

block content
  div.columns.spacer
    div.column.col-12.stories
      if comments
        each comment in comments
          +commentRender(comment, user_vote_mapping, can_downvote)
  if has_next
    +pager('/comments', next_page)